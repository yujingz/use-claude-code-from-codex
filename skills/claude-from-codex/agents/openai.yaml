interface:
  display_name: "Claude from Codex"
  short_description: "Delegate explicit Codex tasks to local Claude Code for parallel investigation, review, or bounded implementation."
  default_prompt: "Run Claude setup diagnostics, then delegate this task with explicit context."
policy:
  allow_implicit_invocation: true
